# This is where the result of the script output.
log/

# Firecrawl CLI working directory (scraped content, cache, install checks).
# Never commit scraped web content or local Firecrawl state.
.firecrawl/
